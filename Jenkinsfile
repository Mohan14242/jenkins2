   pipeline {
       agent {
           label 'mohan'
       }
       stages {
           stage('Build') {
               steps {
                   sh 'echo "Building on Linux"'
               }
           }
       }
   }