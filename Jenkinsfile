pipeline {
    agent {
        label "mohan"
    }
    stages {
        stage("build") {
            steps {
                // Creating a directory named "mohanchiru"
                echo "this is the mohan"
            }
        }
    }
}
